{
  "name": "plate-scanner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Development tooling for PassingPlates web app.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.0.0",
    "vite": "^5.2.8",
    "vitest": "^1.6.0"
  }
}
